# docs/ — ツール連携ファイル置き場

本ディレクトリは **ツール連携が必要な自動生成ファイル** のみを格納する。手動編集禁止。

```
docs/database/schema.dbml   # DB スキーマ（SSOT: apps/api/prisma/schema.prisma、pnpm db:docs で再生成）
```

規約・設計・プロセスの SSOT はすべて `.github/sprint-protocol.md` の「SSOT マップ」を参照。
